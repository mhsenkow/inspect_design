"use client";

import React from "react";

const ConfirmPage = (): React.JSX.Element => {
  return (
    <div id="body">
      <h2>Thanks for registering!</h2>
      <p>
        You are now logged in on the web, so you can interact with posted facts.
        You can also download the app and use that to both follow authors and
        share news yourself!
      </p>
    </div>
  );
};

export default ConfirmPage;
